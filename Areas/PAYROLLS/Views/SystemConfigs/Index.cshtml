@model IEnumerable<IFMIS.Areas.Payroll.Models.SystemConfig>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ConfigName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ConfigValue)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ConfigName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ConfigValue)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.SystemConfigID }) |
                @Html.ActionLink("Details", "Details", new { id=item.SystemConfigID }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.SystemConfigID })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
