@model IEnumerable<IFMIS.Areas.IFMISTZ.Models.InstitutionSubSector>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.InstitutionSector.SectorName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SubSectorName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.InstitutionSector.SectorName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SubSectorName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SubSectorId }) |
            @Html.ActionLink("Details", "Details", new { id=item.SubSectorId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SubSectorId })
        </td>
    </tr>
}

</table>
