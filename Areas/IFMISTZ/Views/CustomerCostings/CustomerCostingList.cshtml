@model IEnumerable<IFMIS.Areas.IFMISTZ.Models.CustomerCosting>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CustomerId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CostingId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CustomerId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CostingId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CustomerCostingId }) |
            @Html.ActionLink("Details", "Details", new { id=item.CustomerCostingId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CustomerCostingId })
        </td>
    </tr>
}

</table>
