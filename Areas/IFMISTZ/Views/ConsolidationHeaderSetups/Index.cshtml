@model IEnumerable<IFMIS.Areas.IFMISTZ.Models.ConsolidationHeaderSetup>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ConsolidationHeaderName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Status)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ConsolidationHeaderName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Status)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.ConsolidationHeaderSetupId }) |
                @Html.ActionLink("Details", "Details", new { id=item.ConsolidationHeaderSetupId }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.ConsolidationHeaderSetupId })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
