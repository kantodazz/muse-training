@model IEnumerable<IFMIS.Areas.IFMISTZ.Models.TraRevenue>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TraRevenueId }) |
            @Html.ActionLink("Details", "Details", new { id=item.TraRevenueId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TraRevenueId })
        </td>
    </tr>
}

</table>
