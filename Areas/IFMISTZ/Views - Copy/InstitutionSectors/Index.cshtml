@model IEnumerable<IFMIS.Areas.IFMISTZ.Models.InstitutionSector>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SectorName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SectorName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SectorId }) |
            @Html.ActionLink("Details", "Details", new { id=item.SectorId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SectorId })
        </td>
    </tr>
}

</table>
